name: "Build doc"
on:
  push:
    branches:    
      - master

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
    - uses: ammaraskar/sphinx-action@master
      with:
        pre-build-command: "apt-get update -y && apt-get install -y git"
        build-command: "make doc"
        docs-folder: "build/html"
